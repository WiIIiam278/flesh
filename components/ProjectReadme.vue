<template>
    <!-- Render the markdown readme -->

</template>

<script setup>
// Get the project prop
const { project } = defineProps({
    project: {
        type: Object,
        required: true,
    }
});
</script>